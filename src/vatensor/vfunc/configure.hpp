#ifndef VATENSOR_UFUNC_CONFIGURE_HPP
#define VATENSOR_UFUNC_CONFIGURE_HPP

namespace va::vfunc {
	// You don't need to call this; it's called at load time automatically.
	void configure();
}

#endif //VATENSOR_UFUNC_CONFIGURE_HPP
