# Expected-failures within the test files that are NOT in skips.txt.
# Each line is a test-ID substring matched by array-api-tests's --xfails-file.
# Comment lines (#) and blank lines are ignored.
#
# Workflow:
#   1. Run the suite (see tests/array_api/README.md).
#   2. New failures (not listed here) break the build → real regression.
#   3. Tests listed here are reported as xfailed; if any start passing they
#      show up as XPASSED — investigate and remove the line.
#   4. Bring a line to zero by either: implementing the missing shim in
#      numdot_xp, fixing a NumDot bug, or filing one (and noting upstream).

# === Section: missing adapter shim (work item: extend numdot_xp) ===
# NumDot doesn't expose meshgrid; would need to either implement it via
# nd primitives or file an issue.
array_api_tests/test_creation_functions.py::test_meshgrid

# === Section: NumDot bug — file issue ===
# nd.full and nd.full_like both error with "Incompatible dimension of
# arrays" when the fill_value is a 0-d complex and the dtype is complex.
# Both work correctly for non-complex inputs.
array_api_tests/test_creation_functions.py::test_full
array_api_tests/test_creation_functions.py::test_full_like

# === Section: NumDot missing function (file issue) ===
# These tests work for their target function but their assertions use
# array-api functions NumDot doesn't expose. Will unblock when those land.
# - test_eye, test_linspace: use xp.where
# - test_asarray_arrays: uses xp.real
array_api_tests/test_creation_functions.py::test_eye
array_api_tests/test_creation_functions.py::test_linspace
array_api_tests/test_creation_functions.py::test_asarray_arrays

# === Section: NumDot precision edges (file issue) ===
# NumDot's va::arange computes num after converting all inputs to its
# process_dtype (the common type). When start/stop are huge ints (>2^53)
# and step is a float, both ints round independently to float64 BEFORE
# the subtraction, losing 8+ bits of precision in the diff. numpy avoids
# this by doing the int subtraction first. Hypothesis finds inputs that
# exceed array-api-tests's ±10% size tolerance.
# Pure-int and pure-float arange now work correctly (200+ examples) thanks
# to the integer-arithmetic and direction-mismatch fixes.
array_api_tests/test_creation_functions.py::test_arange
