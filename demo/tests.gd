extends Node2D

func _ready() -> void:
	# Edit your testing code here
	# But don't submit the changes to the repository!
	print(nd.arange(0, 1000, 1, nd.Int64))
	#print(nd.bitwise_left_shift(nd.array(2, nd.Int8), nd.array(-1, nd.Int8)))
